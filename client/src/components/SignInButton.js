import React from 'react';
import Button from '@mui/material/Button';

const SignInButton = () => {
    return (
        <Button variant="contained" style={{backgroundColor: '#219ebc', color: '#FFFFFF'}}>
            Sign In
        </Button>
    );
}

export default SignInButton

